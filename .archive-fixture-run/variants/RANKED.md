# Ranked variants

Auto-generated by `skills/rank.md` after each inner-loop iteration. Sorted by `measured_lift` (when available) and `composite` otherwise. Rows reflect the latest status per slug.

| Rank | Slug                          | Status         | Composite | Measured lift (95% CI) | Diff lines | Hypothesis source                                          | Description                                                                  |
| ---- | ----------------------------- | -------------- | --------- | ---------------------- | ---------- | ---------------------------------------------------------- | ---------------------------------------------------------------------------- |
| 1    | v0001-enable-signup-submit    | pre_validated  | 0.270     | —                      | 2          | fixture:rage_clicks,fixture:funnel,fixture:page_attention  | Remove `disabled` attr on `/signup` submit (primary blocker)                 |
| —    | v0000-baseline                | proposed       | —         | —                      | 0          | outer_loop:poll                                            | Baseline snapshot                                                            |

## Notes

- v0001 sits below `prevalidation.thresholds.push` (0.35) so it is held for recombination / human review rather than queued to the abtest adapter. The fast-track path (`blocker_removed >= 0.5` + `diff_lines <= 5`) does NOT auto-trigger because `workflow.review_mode` is `manual` (fast-track only fires in branch (b) auto mode, or in branch (d) when `auto_apply_to_repo` is true — both currently disabled).
- A future iteration may recombine v0001 with a CTA-specificity or form-friction variant on `/signup` to push composite above 0.35.
